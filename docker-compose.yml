version: "3"
services:
  editor:
    build:
      dockerfile: Dockerfile.nvim
      args:
        UID: 1000
        GID: 1000
    volumes:
      - ".:/workspaces/${COMPOSE_PROJECT_NAME}"
      - "${SSH_AUTH_SOCK}:/tmp/ssh-agent.sock"
    environment:
      SSH_AUTH_SOCK: "/tmp/ssh-agent.sock"

